<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="pl.akai.bookcrossing.user.UserDaoMapper">

    <resultMap id="baseUserMap" type="pl.akai.bookcrossing.model.User" >
        <id column="id" property="id" jdbcType="INTEGER" />
        <result column="firstName" property="firstName" jdbcType="VARCHAR" />
        <result column="lastName" property="lastName" jdbcType="VARCHAR" />
    </resultMap>

    <select id="getUserById" resultMap="baseUserMap">
        SELECT * FROM tb_user
        WHERE id = #{id}
    </select>

    <insert id="insertUser">
        INSERT INTO tb_user (firstName, lastName)
        VALUES (#{user.firstName}, #{user.lastName});
    </insert>

</mapper>
