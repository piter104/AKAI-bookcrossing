<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="pl.akai.bookcrossing.user.OpinionDaoMapper">

    <resultMap id="baseOpinionMap" type="pl.akai.bookcrossing.model.Opinion" >
        <id column="id" property="id" jdbcType="INTEGER" />
        <result column="rating" property="rating" jdbcType="INTEGER" />
        <result column="description" property="description" jdbcType="VARCHAR" />
        <result column="bookId" property="bookId" jdbcType="INTEGER" />
        <result column="authorId" property="authorId" jdbcType="INTEGER" />
    </resultMap>

    <select id="getOpinionById" resultMap="baseUserMap">
        SELECT * FROM tb_opinion
        WHERE id = #{id}
    </select>

    <insert id="insertOpinion">
        INSERT INTO tb_opinion (rating, description, bookId, authorId)
        VALUES (#{opinion.rating}, #{opinion.description}, #{opinion.bookId}, #{opinion.authorId});
    </insert>

    <update id="updateOpinion">
        UPDATE tb_opinion
        SET rating = #{opinion.rating}, description = #{opinion.description}, bookId = #{opinion.bookId}, authorId = #{opinion.authorId}
        WHERE id = #{opinion.id});
    </update>

</mapper>